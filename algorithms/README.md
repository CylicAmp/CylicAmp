# Algorithms

Production-ready implementations of the mathematical discoveries. Each algorithm embeds the exclusive math logic (digital roots, totient boundaries, grid positions, jump sequences, completion paths) into clean, functional code.

These files are protected as trade secrets under TUTSA. The specific implementation details and methods are confidential.

## Structure
- src/ → Clean algorithm source (confidential)
- tests/ → Verification tests (internal only)
- docs/ → Formal .tex proofs tied to the discovery (copyright on expression)

Every file begins with the CONFIDENTIAL – TRADE SECRET header.

Add new algorithms by converting discoveries one-to-one. The 360 Milestone Overlay is ready for its first implementation.

See ALGORITHM_PROTECTION_STRATEGY.md in the repository root for the full protection approach.
