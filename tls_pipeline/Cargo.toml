[package]
name = "tls_pipeline"
version = "0.1.0"
edition = "2021"

[dependencies]
rustls       = { version = "0.23", default-features = false, features = ["ring", "std"] }
ring         = "0.17"
serde_json   = "1"
x509-parser  = "0.16"
hex          = "0.4"

[dev-dependencies]
rcgen        = "0.13"
